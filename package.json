{
  "engines": {
    "node": "14.16.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon src/built-in.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.9"
  }
}
